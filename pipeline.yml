trigger: 
- main

pool: Unreal

resources:
  repositories:
    - repository: devops
      type: git
      name: plugins-unreal-publishing
      
variables: 
- template: variables.yml@devops

extends:
  template: trigger.yml@devops
